##################################################
# OldMonk Global config file
##################################################


# Define the interested exchanges here. 
# One primary exchange for trading and as many others in spectator mode
# data from exchanges in spectator mode will be considered for trading decision
exchanges:
#   - 'binance': 
#      role: 'secondary'
#      config: 'config/binance.yml'
   - 'cbpro' : 
      role: 'primary'
      config: 'config/cbpro.yml'
  
# TODO: exchanges specific config should follow a common timing, should be driven by global config  
#  backfill:
#    - enabled  : Yes  
#    - period   : 3  #in Days
#    - interval : '5m'  #5m 

stop_loss:
   enabled: true
   smart: true #track price movement and update stop_loss
   rate : 7 #-3%
  
take_profit:
   enabled: true
   rate: 16 # 10percent

#decision engine

decision:
# model : [ML|simple]
#   model    : ML
#   config   : "config/model/model.hdf5"

   model    : simple
   config   :
      #strategy: TRIX_RSI
      #params : {'rsi_oversold_level': 66, 'period': 76, 'trix': 10, 'rsi': 24, 'rsi_overbought_level': 78}
      #strategy: EMA_DEV
     #params : {'ema_sell_s': 85, 'timeout_sell': 40, 'rsi': 24, 'treshold_pct_buy_l': 1.0, 'ema_buy_s': 185, 'timeout_buy': 14, 'period': 165, 'treshold_pct_sell_s': 0.92, 'ema_buy_l': 70, 'treshold_pct_sell_l': 1.35, 'treshold_pct_buy_s': 0.39, 'ema_sell_l': 25}
      strategy : EMA_RSI
      params : {'rsi_bullish_mark': 36, 'rsi': 14, 'ema_s': 84, 'period': 124, 'ema_m': 180, 'ema_l': 152, 'ema_ll': 126}
      #Hall of Fame
      #EMA_RSI: {'rsi': 64, 'ema_s': 44, 'period': 38, 'ema_m': 184, 'ema_l': 156, 'ema_ll': 64}
      #EMA_DEV{'ema_sell_s': 80, 'rsi': 90, 'treshold_pct_buy_l': 0.23, 'ema_buy_s': 130, 'period': 190, 'ema_sell_l': 85, 'ema_buy_l': 115, 'treshold_pct_sell_l': 1.08, 'treshold_pct_buy_s': 1.02, 'treshold_pct_sell_s': 0.65}
#"MINMAX" #<buy hold
#"EMA_RSI" #<buy-hold, high fail%
#TRIX_RSI" #very low profit
#TREND_BOLLINGER"
#TREND_RSI"
simulator:
   enabled : true

genetic_optimizer: 
   enabled : false
   N_POP : 1000
   N_GEN : 100
   N_MP : 10
   config   :
      strategy : EMA_RSI  
      trading : {}
#EOF

